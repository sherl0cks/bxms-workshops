<!-- Main jumbotron for a primary marketing message or call to action -->
<div class="jumbotron">
	<div class="container">
		<h1>Author Rules Your Way</h1>
		<p>With Red Hat BRMS, it is simple to package, version and hot deploy new business rules to your application without downtime. Use the design patterns presented here to greatly reduce the cycle
			time required to move new business rules from design to production, while at the same time ensuring compliance in highly regulated environments.</p>
	</div>
</div>

<div class="container">

	<!-- Using the IDE -->
	<div class="row">
		<div class="text-left">
			<h2 class="section-heading"></h2>
			<hr class="light">
			<p class="text-faded">Provide one clean and simple interface to every application that needs to fire rules. With this pattern, developers invoking a decision services to execute rules only need
				to know three things.</p>
			<ol>
				<li>The name of the process that will orchestrate the rules</li>
				<li>The facts required for the rules to make a decision</li>
				<li>The type of response required from the rules</li>
			</ol>
			<p>Everything else, including how and where the BRMS APIs will create sessions, insert facts and fire rules is an implementation detail that can be injected via the Spring context.</p>
		</div>
	</div>

	<!-- Business Rules Administrator -->
	<div class="row">
		<div class="text-left">
			<h2 class="section-heading">Business Rules Administrator Push TODO</h2>
			<hr class="light">
			<p class="text-faded">Provide one clean and simple interface to every application that needs to fire rules. With this pattern, developers invoking a decision services to execute rules only need
				to know three things.</p>
			<ol>
				<li>The name of the process that will orchestrate the rules</li>
				<li>The facts required for the rules to make a decision</li>
				<li>The type of response required from the rules</li>
			</ol>
			<p>Everything else, including how and where the BRMS APIs will create sessions, insert facts and fire rules is an implementation detail that can be injected via the Spring context.</p>
		</div>
	</div>

	<!-- Business Rules Administrator -->
	<div class="row">
		<div class="text-left">
			<h2 class="section-heading">Considerations for Citi's Network Partitions TODO</h2>
			<hr class="light">
			<p class="text-faded">Provide one clean and simple interface to every application that needs to fire rules. With this pattern, developers invoking a decision services to execute rules only need
				to know three things.</p>
			<ol>
				<li>The name of the process that will orchestrate the rules</li>
				<li>The facts required for the rules to make a decision</li>
				<li>The type of response required from the rules</li>
			</ol>
			<p>Everything else, including how and where the BRMS APIs will create sessions, insert facts and fire rules is an implementation detail that can be injected via the Spring context.</p>
		</div>
	</div>
</div>